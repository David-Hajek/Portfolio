import type { EventMap } from "@threlte/extras";
type SplatStack = {
    mouseX?: number;
    mouseY?: number;
    velocityX?: number;
    velocityY?: number;
};
export declare function usePointer({ force }: {
    force: number;
}): {
    onPointerMove: (event: EventMap["onpointermove"]) => void;
    splatStack: import("svelte/store").Writable<SplatStack[]>;
};
export {};
