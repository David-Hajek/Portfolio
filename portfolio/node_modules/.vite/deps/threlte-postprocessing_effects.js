import {
  BlendFunction,
  BloomEffect,
  BrightnessContrastEffect,
  ChromaticAberrationEffect,
  ColorAverageEffect,
  ColorDepthEffect,
  DepthEffect,
  DepthOfFieldEffect,
  DotScreenEffect,
  Effect,
  EffectAttribute,
  FXAAEffect,
  GodRaysEffect,
  GridEffect,
  HueSaturationEffect,
  LUT3DEffect,
  MaskFunction,
  NoiseEffect,
  OutlineEffect,
  PixelationEffect,
  SMAAEffect,
  SSAOEffect,
  ScanlineEffect,
  SelectiveBloomEffect,
  SepiaEffect,
  ShockWaveEffect,
  TextureEffect,
  TiltShiftEffect,
  ToneMappingEffect,
  VignetteEffect,
  getEffectComposerContext,
  getSelectionContext
} from "./chunk-OCJYH2E2.js";
import {
  useLoader,
  useThrelte
} from "./chunk-GMESNTE2.js";
import "./chunk-2Y2UB3XH.js";
import "./chunk-2XS5AGVA.js";
import {
  check_target,
  hmr,
  legacy_api,
  onMount,
  prop,
  rest_props,
  setup_stores,
  store_get,
  validate_store
} from "./chunk-DTM7CJLO.js";
import "./chunk-U7P2NEEE.js";
import "./chunk-2DGCMXEA.js";
import "./chunk-UQUCXX2V.js";
import {
  FILENAME,
  HMR,
  equals,
  mark_module_end,
  mark_module_start,
  pop,
  push,
  set,
  strict_equals,
  tick,
  user_effect
} from "./chunk-MU4ARNFD.js";
import "./chunk-X4XZK27Q.js";
import "./chunk-RVAV4ZRS.js";
import {
  CanvasTexture,
  Color,
  NearestFilter,
  RepeatWrapping,
  Texture,
  TextureLoader,
  Uniform,
  Vector3
} from "./chunk-ZJ73KPTD.js";
import {
  __publicField
} from "./chunk-SROXYCZ4.js";

// node_modules/threlte-postprocessing/dist/effects/pixelation/PixelationEffect.svelte
mark_module_start();
PixelationEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/pixelation/PixelationEffect.svelte";
function PixelationEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, PixelationEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let granularity = prop($$props, "granularity", 3, 20);
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new PixelationEffect(granularity());
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    _effect.granularity = granularity();
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  PixelationEffect_1 = hmr(PixelationEffect_1, () => PixelationEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = PixelationEffect_1[HMR].source;
    set(PixelationEffect_1[HMR].source, module.default[HMR].original);
  });
}
var PixelationEffect_default = PixelationEffect_1;
mark_module_end(PixelationEffect_1);

// node_modules/threlte-postprocessing/dist/effects/ASCII/effect.js
var fragment = `
uniform sampler2D uCharacters;
uniform float uCharactersCount;
uniform float uCellSize;
uniform bool uInvert;
uniform vec3 uColor;

const vec2 SIZE = vec2(16.);

vec3 greyscale(vec3 color, float strength) {
    float g = dot(color, vec3(0.299, 0.587, 0.114));
    return mix(color, vec3(g), strength);
}

vec3 greyscale(vec3 color) {
    return greyscale(color, 1.0);
}

void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
    vec2 cell = resolution / uCellSize;
    vec2 grid = 1.0 / cell;
    vec2 pixelizedUV = grid * (0.5 + floor(uv / grid));
    vec4 pixelized = texture2D(inputBuffer, pixelizedUV);
    float greyscaled = greyscale(pixelized.rgb).r;

    if (uInvert) {
        greyscaled = 1.0 - greyscaled;
    }

    float characterIndex = floor((uCharactersCount - 1.0) * greyscaled);
    vec2 characterPosition = vec2(mod(characterIndex, SIZE.x), floor(characterIndex / SIZE.y));
    vec2 offset = vec2(characterPosition.x, -characterPosition.y) / SIZE;
    vec2 charUV = mod(uv * (cell / SIZE), 1.0 / SIZE) - vec2(0., 1.0 / SIZE) + offset;
    vec4 asciiCharacter = texture2D(uCharacters, charUV);

    asciiCharacter.rgb = uColor * asciiCharacter.r;
    asciiCharacter.a = pixelized.a;
    outputColor = asciiCharacter;
}
`;
var ASCIIEffect = class extends Effect {
  constructor({ font = "arial", characters = ` .:,'-^=*+?!|0#X%WM@`, fontSize = 54, cellSize = 16, color = "#ffffff", invert = false } = {}) {
    const uniforms = /* @__PURE__ */ new Map([
      ["uCharacters", new Uniform(new Texture())],
      ["uCellSize", new Uniform(cellSize)],
      ["uCharactersCount", new Uniform(characters.length)],
      ["uColor", new Uniform(new Color(color))],
      ["uInvert", new Uniform(invert)]
    ]);
    super("ASCIIEffect", fragment, { uniforms });
    __publicField(this, "state");
    const charactersTextureUniform = this.uniforms.get("uCharacters");
    this.state = {
      font,
      characters,
      fontSize,
      cellSize,
      color,
      invert
    };
    if (charactersTextureUniform) {
      charactersTextureUniform.value = this.createCharactersTexture(characters, font, fontSize);
    }
  }
  updateUniform(key, value) {
    const uniform = this.uniforms.get(key);
    if (uniform) {
      uniform.value = value;
    }
  }
  updateUniforms() {
    var _a;
    this.updateUniform("uCharacters", this.createCharactersTexture(this.state.characters, this.state.font, this.state.fontSize));
    this.updateUniform("uInvert", this.state.invert);
    this.updateUniform("uColor", new Color(this.state.color));
    this.updateUniform("uCellSize", this.state.cellSize);
    this.updateUniform("uCharactersCount", (_a = this.state.characters) == null ? void 0 : _a.length);
  }
  updateState(state) {
    this.state = {
      ...this.state,
      ...state
    };
    this.updateUniforms();
  }
  /** Draws the characters on a Canvas and returns a texture */
  createCharactersTexture(characters, font, fontSize) {
    const canvas = document.createElement("canvas");
    const SIZE = 1024;
    const MAX_PER_ROW = 16;
    const CELL = SIZE / MAX_PER_ROW;
    canvas.width = canvas.height = SIZE;
    const texture = new CanvasTexture(canvas, void 0, RepeatWrapping, RepeatWrapping, NearestFilter, NearestFilter);
    const context = canvas.getContext("2d");
    if (!context) {
      throw new Error("Context not available");
    }
    context.clearRect(0, 0, SIZE, SIZE);
    context.font = `${fontSize}px ${font}`;
    context.textAlign = "center";
    context.textBaseline = "middle";
    context.fillStyle = "#fff";
    for (let i = 0; i < characters.length; i++) {
      const char = characters[i];
      const x = i % MAX_PER_ROW;
      const y = Math.floor(i / MAX_PER_ROW);
      context.fillText(char, x * CELL + CELL / 2, y * CELL + CELL / 2);
    }
    texture.needsUpdate = true;
    return texture;
  }
};

// node_modules/threlte-postprocessing/dist/effects/ASCII/ASCIIEffect.svelte
mark_module_start();
ASCIIEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/ASCII/ASCIIEffect.svelte";
function ASCIIEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ASCIIEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new ASCIIEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    _effect.updateState(props);
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  ASCIIEffect_1 = hmr(ASCIIEffect_1, () => ASCIIEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = ASCIIEffect_1[HMR].source;
    set(ASCIIEffect_1[HMR].source, module.default[HMR].original);
  });
}
var ASCIIEffect_default = ASCIIEffect_1;
mark_module_end(ASCIIEffect_1);

// node_modules/threlte-postprocessing/dist/effects/bloom/BloomEffect.svelte
mark_module_start();
BloomEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/bloom/BloomEffect.svelte";
function BloomEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, BloomEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let blendFunction = prop($$props, "blendFunction", 19, () => BlendFunction.ADD), mipmapBlur = prop($$props, "mipmapBlur", 3, true), props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "blendFunction",
      "mipmapBlur"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new BloomEffect({
    blendFunction: blendFunction(),
    mipmapBlur: mipmapBlur(),
    ...props
  });
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      width,
      height,
      intensity,
      resolutionScale,
      radius,
      luminanceThreshold,
      luminanceSmoothing,
      resolutionX,
      resolutionY,
      levels,
      kernelSize
    } = props;
    width && height && _effect.setSize(width, height);
    intensity && _effect.setIntensity(intensity);
    resolutionScale && _effect.setResolutionScale(resolutionScale);
    if (radius) _effect.mipmapBlurPass.radius = radius;
    if (luminanceSmoothing) _effect.luminanceMaterial.smoothing = luminanceSmoothing;
    if (luminanceThreshold) _effect.luminanceMaterial.threshold = luminanceThreshold;
    if (blendFunction()) _effect.blendMode.blendFunction = blendFunction();
    if (resolutionX) _effect.resolution.width = resolutionX;
    if (resolutionY) _effect.resolution.height = resolutionY;
    if (levels) _effect.mipmapBlurPass.levels = levels;
    if (kernelSize) _effect.kernelSize = kernelSize;
    _effect.mipmapBlurPass.enabled = mipmapBlur();
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  BloomEffect_1 = hmr(BloomEffect_1, () => BloomEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = BloomEffect_1[HMR].source;
    set(BloomEffect_1[HMR].source, module.default[HMR].original);
  });
}
var BloomEffect_default = BloomEffect_1;
mark_module_end(BloomEffect_1);

// node_modules/threlte-postprocessing/dist/effects/brightness-contrast/BrightnessContrastEffect.svelte
mark_module_start();
BrightnessContrastEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/brightness-contrast/BrightnessContrastEffect.svelte";
function BrightnessContrastEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, BrightnessContrastEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let brightness = prop($$props, "brightness", 3, 0), contrast = prop($$props, "contrast", 3, 0), blendFunction = prop($$props, "blendFunction", 19, () => BlendFunction.ALPHA);
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new BrightnessContrastEffect({
    brightness: brightness(),
    contrast: contrast(),
    blendFunction: blendFunction()
  });
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    if (brightness()) _effect.brightness = brightness();
    if (contrast()) _effect.contrast = contrast();
    if (blendFunction()) _effect.blendMode.blendFunction = blendFunction();
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  BrightnessContrastEffect_1 = hmr(BrightnessContrastEffect_1, () => BrightnessContrastEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = BrightnessContrastEffect_1[HMR].source;
    set(BrightnessContrastEffect_1[HMR].source, module.default[HMR].original);
  });
}
var BrightnessContrastEffect_default = BrightnessContrastEffect_1;
mark_module_end(BrightnessContrastEffect_1);

// node_modules/threlte-postprocessing/dist/effects/chromatic-aberration/ChromaticAberrationEffect.svelte
mark_module_start();
ChromaticAberrationEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/chromatic-aberration/ChromaticAberrationEffect.svelte";
function ChromaticAberrationEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ChromaticAberrationEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new ChromaticAberrationEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      modulationOffset,
      radialModulation,
      offset,
      blendFunction
    } = props;
    if (offset) _effect.offset = offset;
    if (modulationOffset) _effect.modulationOffset = modulationOffset;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    _effect.radialModulation = radialModulation;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  ChromaticAberrationEffect_1 = hmr(ChromaticAberrationEffect_1, () => ChromaticAberrationEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = ChromaticAberrationEffect_1[HMR].source;
    set(ChromaticAberrationEffect_1[HMR].source, module.default[HMR].original);
  });
}
var ChromaticAberrationEffect_default = ChromaticAberrationEffect_1;
mark_module_end(ChromaticAberrationEffect_1);

// node_modules/threlte-postprocessing/dist/effects/color-average/ColorAverageEffect.svelte
mark_module_start();
ColorAverageEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/color-average/ColorAverageEffect.svelte";
function ColorAverageEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ColorAverageEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new ColorAverageEffect($$props.blendFunction);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    if ($$props.blendFunction) _effect.blendMode.blendFunction = $$props.blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  ColorAverageEffect_1 = hmr(ColorAverageEffect_1, () => ColorAverageEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = ColorAverageEffect_1[HMR].source;
    set(ColorAverageEffect_1[HMR].source, module.default[HMR].original);
  });
}
var ColorAverageEffect_default = ColorAverageEffect_1;
mark_module_end(ColorAverageEffect_1);

// node_modules/threlte-postprocessing/dist/effects/color-depth/ColorDepthEffect.svelte
mark_module_start();
ColorDepthEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/color-depth/ColorDepthEffect.svelte";
function ColorDepthEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ColorDepthEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new ColorDepthEffect({
    blendFunction: $$props.blendFunction,
    bits: $$props.bits
  });
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    if (strict_equals($$props.bits, void 0, false)) _effect.setBitDepth($$props.bits);
    if ($$props.blendFunction) _effect.blendMode.blendFunction = $$props.blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  ColorDepthEffect_1 = hmr(ColorDepthEffect_1, () => ColorDepthEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = ColorDepthEffect_1[HMR].source;
    set(ColorDepthEffect_1[HMR].source, module.default[HMR].original);
  });
}
var ColorDepthEffect_default = ColorDepthEffect_1;
mark_module_end(ColorDepthEffect_1);

// node_modules/threlte-postprocessing/dist/effects/depth/DepthEffect.svelte
mark_module_start();
DepthEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/depth/DepthEffect.svelte";
function DepthEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, DepthEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new DepthEffect({
    blendFunction: $$props.blendFunction,
    inverted: $$props.inverted
  });
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    if (strict_equals($$props.inverted, void 0, false)) _effect.inverted = $$props.inverted;
    if ($$props.blendFunction) _effect.blendMode.blendFunction = $$props.blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  DepthEffect_1 = hmr(DepthEffect_1, () => DepthEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = DepthEffect_1[HMR].source;
    set(DepthEffect_1[HMR].source, module.default[HMR].original);
  });
}
var DepthEffect_default = DepthEffect_1;
mark_module_end(DepthEffect_1);

// node_modules/threlte-postprocessing/dist/effects/depth-of-field/DepthOfFieldEffect.svelte
mark_module_start();
DepthOfFieldEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/depth-of-field/DepthOfFieldEffect.svelte";
function DepthOfFieldEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, DepthOfFieldEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const { camera } = useThrelte();
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new DepthOfFieldEffect($context().camera ?? camera.current, props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      depthTexture,
      blendFunction,
      worldFocusDistance,
      worldFocusRange,
      focusDistance,
      focusRange,
      focalLength,
      bokehScale,
      resolutionScale,
      resolutionX,
      resolutionY,
      width,
      height,
      target
    } = props;
    width && height && _effect.setSize(width, height);
    if ($context().camera) _effect.mainCamera = $context().camera;
    if (strict_equals(worldFocusDistance, void 0, false)) _effect.cocMaterial.worldFocusDistance = worldFocusDistance;
    if (strict_equals(worldFocusRange, void 0, false)) _effect.cocMaterial.worldFocusRange = worldFocusRange;
    if (strict_equals(focusDistance, void 0, false)) _effect.cocMaterial.focusDistance = focusDistance;
    if (strict_equals(focusRange, void 0, false)) _effect.cocMaterial.focusRange = focusRange;
    if (strict_equals(focalLength, void 0, false)) _effect.cocMaterial.focalLength = focalLength;
    if (strict_equals(resolutionScale, void 0, false)) _effect.resolution.scale = resolutionScale;
    if (strict_equals(bokehScale, void 0, false)) _effect.bokehScale = bokehScale;
    if (resolutionX && resolutionY) {
      _effect.resolution.width = resolutionX;
      _effect.resolution.height = resolutionY;
    }
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    if (equals(target, null, false) || equals(target, void 0, false)) _effect.target = new Vector3();
    if (depthTexture) _effect.setDepthTexture(depthTexture.texture, depthTexture.packing);
    const maskPass = _effect.maskPass;
    maskPass.maskFunction = MaskFunction.MULTIPLY_RGB_SET_ALPHA;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  DepthOfFieldEffect_1 = hmr(DepthOfFieldEffect_1, () => DepthOfFieldEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = DepthOfFieldEffect_1[HMR].source;
    set(DepthOfFieldEffect_1[HMR].source, module.default[HMR].original);
  });
}
var DepthOfFieldEffect_default = DepthOfFieldEffect_1;
mark_module_end(DepthOfFieldEffect_1);

// node_modules/threlte-postprocessing/dist/effects/dot-screen/DotScreenEffect.svelte
mark_module_start();
DotScreenEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/dot-screen/DotScreenEffect.svelte";
function DotScreenEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, DotScreenEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new DotScreenEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction, scale, angle } = props;
    if (strict_equals(angle, void 0, false)) _effect.angle = angle;
    if (strict_equals(scale, void 0, false)) _effect.scale = scale;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  DotScreenEffect_1 = hmr(DotScreenEffect_1, () => DotScreenEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = DotScreenEffect_1[HMR].source;
    set(DotScreenEffect_1[HMR].source, module.default[HMR].original);
  });
}
var DotScreenEffect_default = DotScreenEffect_1;
mark_module_end(DotScreenEffect_1);

// node_modules/threlte-postprocessing/dist/effects/FXAA/FXAAEffect.svelte
mark_module_start();
FXAAEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/FXAA/FXAAEffect.svelte";
function FXAAEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, FXAAEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new FXAAEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction } = props;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  FXAAEffect_1 = hmr(FXAAEffect_1, () => FXAAEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = FXAAEffect_1[HMR].source;
    set(FXAAEffect_1[HMR].source, module.default[HMR].original);
  });
}
var FXAAEffect_default = FXAAEffect_1;
mark_module_end(FXAAEffect_1);

// node_modules/threlte-postprocessing/dist/effects/god-rays/GodRaysEffect.svelte
mark_module_start();
GodRaysEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/god-rays/GodRaysEffect.svelte";
function GodRaysEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, GodRaysEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "sun"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new GodRaysEffect($context().camera ?? void 0, $$props.sun, props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      blendFunction,
      // TODO: Handle `clampMax`
      // clampMax,
      density,
      decay,
      exposure,
      height,
      kernelSize,
      resolutionScale,
      resolutionX,
      resolutionY,
      samples,
      weight,
      width,
      blur
    } = props;
    width && height && _effect.setSize(width, height);
    if (strict_equals(resolutionScale, void 0, false)) _effect.setResolutionScale(resolutionScale);
    if (resolutionX && resolutionY) {
      _effect.resolution.width = resolutionX;
      _effect.resolution.height = resolutionY;
    }
    if (kernelSize) _effect.kernelSize = kernelSize;
    if (strict_equals(samples, void 0, false)) _effect.godRaysMaterial.samples = samples;
    if (strict_equals(weight, void 0, false)) _effect.godRaysMaterial.weight = weight;
    if (strict_equals(decay, void 0, false)) _effect.godRaysMaterial.decay = decay;
    if (strict_equals(exposure, void 0, false)) _effect.godRaysMaterial.exposure = exposure;
    if (strict_equals(density, void 0, false)) _effect.godRaysMaterial.density = density;
    if ($$props.sun) _effect.lightSource = $$props.sun;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    if (strict_equals(blur, void 0, false)) _effect.blur = blur;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  GodRaysEffect_1 = hmr(GodRaysEffect_1, () => GodRaysEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = GodRaysEffect_1[HMR].source;
    set(GodRaysEffect_1[HMR].source, module.default[HMR].original);
  });
}
var GodRaysEffect_default = GodRaysEffect_1;
mark_module_end(GodRaysEffect_1);

// node_modules/threlte-postprocessing/dist/effects/grid/GridEffect.svelte
mark_module_start();
GridEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/grid/GridEffect.svelte";
function GridEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, GridEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "size"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new GridEffect(props);
  onMount(() => {
    tick().then(() => {
      if ($$props.size) _effect.setSize($$props.size.width, $$props.size.height);
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction, scale, lineWidth } = props;
    if (strict_equals(lineWidth, void 0, false)) _effect.lineWidth = lineWidth;
    if (strict_equals(scale, void 0, false)) _effect.scale = scale;
    if ($$props.size) _effect.setSize($$props.size.width, $$props.size.height);
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  GridEffect_1 = hmr(GridEffect_1, () => GridEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = GridEffect_1[HMR].source;
    set(GridEffect_1[HMR].source, module.default[HMR].original);
  });
}
var GridEffect_default = GridEffect_1;
mark_module_end(GridEffect_1);

// node_modules/threlte-postprocessing/dist/effects/hue-saturation/HueSaturationEffect.svelte
mark_module_start();
HueSaturationEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/hue-saturation/HueSaturationEffect.svelte";
function HueSaturationEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, HueSaturationEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new HueSaturationEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction, hue, saturation } = props;
    if (strict_equals(saturation, void 0, false)) _effect.saturation = saturation;
    if (strict_equals(hue, void 0, false)) _effect.hue = hue;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  HueSaturationEffect_1 = hmr(HueSaturationEffect_1, () => HueSaturationEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = HueSaturationEffect_1[HMR].source;
    set(HueSaturationEffect_1[HMR].source, module.default[HMR].original);
  });
}
var HueSaturationEffect_default = HueSaturationEffect_1;
mark_module_end(HueSaturationEffect_1);

// node_modules/threlte-postprocessing/dist/effects/LUT/LUTEffect.svelte
mark_module_start();
LUTEffect[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/LUT/LUTEffect.svelte";
function LUTEffect($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, LUTEffect);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "lut"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new LUT3DEffect($$props.lut, props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction, tetrahedralInterpolation } = (
      // Can be set only on initialization
      // inputColorSpace
      props
    );
    if ($$props.lut) _effect.lut = $$props.lut;
    if (strict_equals(tetrahedralInterpolation, void 0, false)) _effect.tetrahedralInterpolation = tetrahedralInterpolation;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  LUTEffect = hmr(LUTEffect, () => LUTEffect[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = LUTEffect[HMR].source;
    set(LUTEffect[HMR].source, module.default[HMR].original);
  });
}
var LUTEffect_default = LUTEffect;
mark_module_end(LUTEffect);

// node_modules/threlte-postprocessing/dist/effects/lens-flare/effect.js
var LensFlareShader = {
  fragmentShader: (
    /* glsl */
    `
    uniform float time;
    uniform vec2 lensPosition;
    uniform vec2 screenRes;
    uniform vec3 colorGain;
    uniform float starPoints;
    uniform float glareSize;
    uniform float flareSize;
    uniform float flareSpeed;
    uniform float flareShape;
    uniform float haloScale;
    uniform float opacity;
    uniform bool animated;
    uniform bool anamorphic;
    uniform bool enabled;
    uniform bool secondaryGhosts;
    uniform bool starBurst;
    uniform float ghostScale;
    uniform bool aditionalStreaks;
    uniform sampler2D lensDirtTexture;
    vec2 vTexCoord;
    
    float rand(float n){return fract(sin(n) * 43758.5453123);}

    float noise(float p){
      float fl = floor(p);
      float fc = fract(p);
      return mix(rand(fl),rand(fl + 1.0), fc);
    }

    vec3 hsv2rgb(vec3 c)
    {
      vec4 k = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);
      vec3 p = abs(fract(c.xxx + k.xyz) * 6.0 - k.www);
      return c.z * mix(k.xxx, clamp(p - k.xxx, 0.0, 1.0), c.y);
    }

    float saturate(float x)
    {
      return clamp(x, 0.,1.);
    }

    vec2 rotateUV(vec2 uv, float rotation)
    {
      return vec2(
          cos(rotation) * uv.x + sin(rotation) * uv.y,
          cos(rotation) * uv.y - sin(rotation) * uv.x
      );
    }

    // Based on https://www.shadertoy.com/view/XtKfRV
    vec3 drawflare(vec2 p, float intensity, float rnd, float speed, int id)
    {
      float flarehueoffset = (1. / 32.) * float(id) * 0.1;
      float lingrad = distance(vec2(0.), p);
      float expgrad = 1. / exp(lingrad * (fract(rnd) * 0.66 + 0.33));
      vec3 colgrad = hsv2rgb(vec3( fract( (expgrad * 8.) + speed * flareSpeed + flarehueoffset), pow(1.-abs(expgrad*2.-1.), 0.45), 20.0 * expgrad * intensity)); //rainbow spectrum effect

      float internalStarPoints;

      if(anamorphic){
        internalStarPoints = 1.0;
      } else{
        internalStarPoints = starPoints;
      }
      
      float blades = length(p * flareShape * sin(internalStarPoints * atan(p.x, p.y)));
      
      float comp = pow(1.-saturate(blades), ( anamorphic ? 100. : 12.));
      comp += saturate(expgrad-0.9) * 3.;
      comp = pow(comp * expgrad, 8. + (1.-intensity) * 5.);
      
      if(flareSpeed > 0.0){
        return vec3(comp) * colgrad;
      } else{
        return vec3(comp) * flareSize * 15.;
      }
    }

    float dist(vec3 a, vec3 b) { return abs(a.x - b.x) + abs(a.y - b.y) + abs(a.z - b.z); }

    vec3 saturate(vec3 x)
    {
      return clamp(x, vec3(0.0), vec3(1.0));
    }

    // Based on https://www.shadertoy.com/view/XtKfRV
    float glare(vec2 uv, vec2 pos, float size)
    {
      vec2 main;

      if(animated){
        main = rotateUV(uv-pos, time * 0.1);      
      } else{
        main = uv-pos;     
      }
      
      float ang = atan(main.y, main.x) * (anamorphic ? 1.0 : starPoints);
      float dist = length(main); 
      dist = pow(dist, .9);
      
      float f0 = 1.0/(length(uv-pos)*(1.0/size*16.0)+.2);

      return f0+f0*(sin((ang))*.2 +.3);
    }

    float sdHex(vec2 p){
      p = abs(p);
      vec2 q = vec2(p.x*2.0*0.5773503, p.y + p.x*0.5773503);
      return dot(step(q.xy,q.yx), 1.0-q.yx);
    }

    //Based on https://www.shadertoy.com/view/dllSRX
    float fpow(float x, float k){
      return x > k ? pow((x-k)/(1.0-k),2.0) : 0.0;
    }

    vec3 renderhex(vec2 uv, vec2 p, float s, vec3 col){
      uv -= p;
      if (abs(uv.x) < 0.2*s && abs(uv.y) < 0.2*s){
          return mix(vec3(0),mix(vec3(0),col,0.1 + fpow(length(uv/s),0.1)*10.0),smoothstep(0.0,0.1,sdHex(uv*20.0/s)));
      }
      return vec3(0);
    }

    // Based on https://www.shadertoy.com/view/4sX3Rs
    vec3 LensFlare(vec2 uv, vec2 pos)
    {
      vec2 main = uv-pos;
      vec2 uvd = uv*(length(uv));
      
      float ang = atan(main.x,main.y);
      
      float f0 = .3/(length(uv-pos)*16.0+1.0);
      
      f0 = f0*(sin(noise(sin(ang*3.9-(animated ? time : 0.0) * 0.3) * starPoints))*.2 );
      
      float f1 = max(0.01-pow(length(uv+1.2*pos),1.9),.0)*7.0;

      float f2 = max(.9/(10.0+32.0*pow(length(uvd+0.99*pos),2.0)),.0)*0.35;
      float f22 = max(.9/(11.0+32.0*pow(length(uvd+0.85*pos),2.0)),.0)*0.23;
      float f23 = max(.9/(12.0+32.0*pow(length(uvd+0.95*pos),2.0)),.0)*0.6;
      
      vec2 uvx = mix(uv,uvd, 0.1);
      
      float f4 = max(0.01-pow(length(uvx+0.4*pos),2.9),.0)*4.02;
      float f42 = max(0.0-pow(length(uvx+0.45*pos),2.9),.0)*4.1;
      float f43 = max(0.01-pow(length(uvx+0.5*pos),2.9),.0)*4.6;
      
      uvx = mix(uv,uvd,-.4);
      
      float f5 = max(0.01-pow(length(uvx+0.1*pos),5.5),.0)*2.0;
      float f52 = max(0.01-pow(length(uvx+0.2*pos),5.5),.0)*2.0;
      float f53 = max(0.01-pow(length(uvx+0.1*pos),5.5),.0)*2.0;
      
      uvx = mix(uv,uvd, 2.1);
      
      float f6 = max(0.01-pow(length(uvx-0.3*pos),1.61),.0)*3.159;
      float f62 = max(0.01-pow(length(uvx-0.325*pos),1.614),.0)*3.14;
      float f63 = max(0.01-pow(length(uvx-0.389*pos),1.623),.0)*3.12;
      
      vec3 c = vec3(glare(uv,pos, glareSize));

      vec2 prot;

      if(animated){
        prot = rotateUV(uv - pos, (time * 0.1));  
      } else if(anamorphic){
        prot = rotateUV(uv - pos, 1.570796);     
      } else {
        prot = uv - pos;
      }

      c += drawflare(prot, (anamorphic ? flareSize * 10. : flareSize), 0.1, time, 1);
      
      c.r+=f1+f2+f4+f5+f6; c.g+=f1+f22+f42+f52+f62; c.b+=f1+f23+f43+f53+f63;
      c = c*1.3 * vec3(length(uvd)+.09);
      c+=vec3(f0);
      
      return c;
    }

    vec3 cc(vec3 color, float factor,float factor2)
    {
      float w = color.x+color.y+color.z;
      return mix(color,vec3(w)*factor,w*factor2);
    }    

    float rnd(vec2 p)
    {
      float f = fract(sin(dot(p, vec2(12.1234, 72.8392) )*45123.2));
      return f;   
    }

    float rnd(float w)
    {
      float f = fract(sin(w)*1000.);
      return f;   
    }

    float regShape(vec2 p, int N)
    {
      float f;
      
      float a=atan(p.x,p.y)+.2;
      float b=6.28319/float(N);
      f=smoothstep(.5,.51, cos(floor(.5+a/b)*b-a)*length(p.xy)* 2.0  -ghostScale);
          
      return f;
    }

    // Based on https://www.shadertoy.com/view/Xlc3D2
    vec3 circle(vec2 p, float size, float decay, vec3 color, vec3 color2, float dist, vec2 position)
    {
      float l = length(p + position*(dist*2.))+size/2.;
      float l2 = length(p + position*(dist*4.))+size/3.;
      
      float c = max(0.01-pow(length(p + position*dist), size*ghostScale), 0.0)*10.;
      float c1 = max(0.001-pow(l-0.3, 1./40.)+sin(l*20.), 0.0)*3.;
      float c2 =  max(0.09/pow(length(p-position*dist/.5)*1., .95), 0.0)/20.;
      float s = max(0.02-pow(regShape(p*5. + position*dist*5. + decay, 6) , 1.), 0.0)*1.5;
      
      color = cos(vec3(0.44, .24, .2)*16. + dist/8.)*0.5+.5;
      vec3 f = c*color;
      f += c1*color;
      f += c2*color;  
      f +=  s*color;
      return f;
    }

    vec4 getLensColor(float x){
      return vec4(vec3(mix(mix(mix(mix(mix(mix(mix(mix(mix(mix(mix(mix(mix(mix(mix(vec3(0., 0., 0.),
        vec3(0., 0., 0.), smoothstep(0.0, 0.063, x)),
        vec3(0., 0., 0.), smoothstep(0.063, 0.125, x)),
        vec3(0.0, 0., 0.), smoothstep(0.125, 0.188, x)),
        vec3(0.188, 0.131, 0.116), smoothstep(0.188, 0.227, x)),
        vec3(0.31, 0.204, 0.537), smoothstep(0.227, 0.251, x)),
        vec3(0.192, 0.106, 0.286), smoothstep(0.251, 0.314, x)),
        vec3(0.102, 0.008, 0.341), smoothstep(0.314, 0.392, x)),
        vec3(0.086, 0.0, 0.141), smoothstep(0.392, 0.502, x)),
        vec3(1.0, 0.31, 0.0), smoothstep(0.502, 0.604, x)),
        vec3(.1, 0.1, 0.1), smoothstep(0.604, 0.643, x)),
        vec3(1.0, 0.929, 0.0), smoothstep(0.643, 0.761, x)),
        vec3(1.0, 0.086, 0.424), smoothstep(0.761, 0.847, x)),
        vec3(1.0, 0.49, 0.0), smoothstep(0.847, 0.89, x)),
        vec3(0.945, 0.275, 0.475), smoothstep(0.89, 0.941, x)),
        vec3(0.251, 0.275, 0.796), smoothstep(0.941, 1.0, x))),
      1.0);
    }

    float dirtNoise(vec2 p){
      vec2 f = fract(p);
      f = (f * f) * (3.0 - (2.0 * f));    
      float n = dot(floor(p), vec2(1.0, 157.0));
      vec4 a = fract(sin(vec4(n + 0.0, n + 1.0, n + 157.0, n + 158.0)) * 43758.5453123);
      return mix(mix(a.x, a.y, f.x), mix(a.z, a.w, f.x), f.y);
    } 

    float fbm(vec2 p){
      const mat2 m = mat2(0.80, -0.60, 0.60, 0.80);
      float f = 0.0;
      f += 0.5000*dirtNoise(p); p = m*p*2.02;
      f += 0.2500*dirtNoise(p); p = m*p*2.03;
      f += 0.1250*dirtNoise(p); p = m*p*2.01;
      f += 0.0625*dirtNoise(p);
      return f/0.9375;
    }

    vec4 getLensStar(vec2 p){
      vec2 pp = (p - vec2(0.5)) * 2.0;
      float a = atan(pp.y, pp.x);
      vec4 cp = vec4(sin(a * 1.0), length(pp), sin(a * 13.0), sin(a * 53.0));
      float d = sin(clamp(pow(length(vec2(0.5) - p) * 0.5 + haloScale /2., 5.0), 0.0, 1.0) * 3.14159);
      vec3 c = vec3(d) * vec3(fbm(cp.xy * 16.0) * fbm(cp.zw * 9.0) * max(max(max(max(0.5, sin(a * 1.0)), sin(a * 3.0) * 0.8), sin(a * 7.0) * 0.8), sin(a * 9.0) * 10.6));
      c *= vec3(mix(2.0, (sin(length(pp.xy) * 256.0) * 0.5) + 0.5, sin((clamp((length(pp.xy) - 0.875) / 0.1, 0.0, 1.0) + 0.0) * 2.0 * 3.14159) * 1.5) + 0.5) * 0.3275;
      return vec4(vec3(c * 1.0), d);	
    }

    vec4 getLensDirt(vec2 p){
      p.xy += vec2(fbm(p.yx * 3.0), fbm(p.yx * 2.0)) * 0.0825;
      vec3 o = vec3(mix(0.125, 0.25, max(max(smoothstep(0.1, 0.0, length(p - vec2(0.25))),
                                            smoothstep(0.4, 0.0, length(p - vec2(0.75)))),
                                            smoothstep(0.8, 0.0, length(p - vec2(0.875, 0.125))))));
      o += vec3(max(fbm(p * 1.0) - 0.5, 0.0)) * 0.5;
      o += vec3(max(fbm(p * 2.0) - 0.5, 0.0)) * 0.5;
      o += vec3(max(fbm(p * 4.0) - 0.5, 0.0)) * 0.25;
      o += vec3(max(fbm(p * 8.0) - 0.75, 0.0)) * 1.0;
      o += vec3(max(fbm(p * 16.0) - 0.75, 0.0)) * 0.75;
      o += vec3(max(fbm(p * 64.0) - 0.75, 0.0)) * 0.5;
      return vec4(clamp(o, vec3(0.15), vec3(1.0)), 1.0);	
    }

    vec4 textureLimited(sampler2D tex, vec2 texCoord){
      if(((texCoord.x < 0.) || (texCoord.y < 0.)) || ((texCoord.x > 1.) || (texCoord.y > 1.))){
        return vec4(0.0);
      }else{
        return texture(tex, texCoord); 
      }
    }

    vec4 textureDistorted(sampler2D tex, vec2 texCoord, vec2 direction, vec3 distortion) {
      return vec4(textureLimited(tex, (texCoord + (direction * distortion.r))).r,
                  textureLimited(tex, (texCoord + (direction * distortion.g))).g,
                  textureLimited(tex, (texCoord + (direction * distortion.b))).b,
                  1.0);
    }

    // Based on https://www.shadertoy.com/view/4sK3W3
    vec4 getStartBurst(){
      vec2 aspectTexCoord = vec2(1.0) - (((vTexCoord - vec2(0.5)) * vec2(1.0)) + vec2(0.5)); 
      vec2 texCoord = vec2(1.0) - vTexCoord; 
      vec2 ghostVec = (vec2(0.5) - texCoord) * 0.3 - lensPosition;
      vec2 ghostVecAspectNormalized = normalize(ghostVec * vec2(1.0)) * vec2(1.0);
      vec2 haloVec = normalize(ghostVec) * 0.6;
      vec2 haloVecAspectNormalized = ghostVecAspectNormalized * 0.6;
      vec2 texelSize = vec2(1.0) / vec2(screenRes.xy);
      vec3 distortion = vec3(-(texelSize.x * 1.5), 0.2, texelSize.x * 1.5);
      vec4 c = vec4(0.0);
      for (int i = 0; i < 8; i++) {
        vec2 offset = texCoord + (ghostVec * float(i));
        c += textureDistorted(lensDirtTexture, offset, ghostVecAspectNormalized, distortion) * pow(max(0.0, 1.0 - (length(vec2(0.5) - offset) / length(vec2(0.5)))), 10.0);
      }                       
      vec2 haloOffset = texCoord + haloVecAspectNormalized; 
      return (c * getLensColor((length(vec2(0.5) - aspectTexCoord) / length(vec2(haloScale))))) + 
            (textureDistorted(lensDirtTexture, haloOffset, ghostVecAspectNormalized, distortion) * pow(max(0.0, 1.0 - (length(vec2(0.5) - haloOffset) / length(vec2(0.5)))), 10.0));
    } 

    void mainImage(vec4 inputColor, vec2 uv, out vec4 outputColor)
    {
      vec2 myUV = uv -0.5;
      myUV.y *= screenRes.y/screenRes.x;
      vec2 finalLensPosition = lensPosition * 0.5;
      finalLensPosition.y *= screenRes.y/screenRes.x;
      
      //First Lens flare pass
      vec3 finalColor = LensFlare(myUV, finalLensPosition) * 20.0 * colorGain / 256.;

      //Aditional streaks
      if(aditionalStreaks){
        vec3 circColor = vec3(0.9, 0.2, 0.1);
        vec3 circColor2 = vec3(0.3, 0.1, 0.9);

        for(float i=0.;i<10.;i++){
          finalColor += circle(myUV, pow(rnd(i*2000.)*2.8, .1)+1.41, 0.0, circColor+i , circColor2+i, rnd(i*20.)*3.+0.2-.5, lensPosition);
        }
      }

      //Alternative ghosts
      if(secondaryGhosts){
        vec3 altGhosts = vec3(0);
        altGhosts += renderhex(myUV, -lensPosition*0.25, ghostScale * 1.4, vec3(0.25,0.35,0));
        altGhosts += renderhex(myUV, lensPosition*0.25, ghostScale * 0.5, vec3(1,0.5,0.5));
        altGhosts += renderhex(myUV, lensPosition*0.1, ghostScale * 1.6, vec3(1,1,1));
        altGhosts += renderhex(myUV, lensPosition*1.8, ghostScale * 2.0, vec3(0,0.5,0.75));
        altGhosts += renderhex(myUV, lensPosition*1.25, ghostScale * 0.8, vec3(1,1,0.5));
        altGhosts += renderhex(myUV, -lensPosition*1.25, ghostScale * 5.0, vec3(0.5,0.5,0.25));
        
        //Circular ghosts
        altGhosts += fpow(1.0 - abs(distance(lensPosition*0.8,myUV) - 0.7),0.985)*colorGain / 2100.;
        finalColor += altGhosts;
      }
      

      //Starburst                     
      if(starBurst){
        vTexCoord = myUV + 0.5;
        vec4 lensMod = getLensDirt(myUV);
        float tooBright = 1.0 - (clamp(0.5, 0.0, 0.5) * 2.0); 
        float tooDark = clamp(0.5 - 0.5, 0.0, 0.5) * 2.0;
        lensMod += mix(lensMod, pow(lensMod * 2.0, vec4(2.0)) * 0.5, tooBright);
        float lensStarRotationAngle = ((myUV.x + myUV.y)) * (1.0 / 6.0);
        vec2 lensStarTexCoord = (mat2(cos(lensStarRotationAngle), -sin(lensStarRotationAngle), sin(lensStarRotationAngle), cos(lensStarRotationAngle)) * vTexCoord);
        lensMod += getLensStar(lensStarTexCoord) * 2.;
        
        finalColor += clamp((lensMod.rgb * getStartBurst().rgb ), 0.01, 1.0);
      }

      //Final composed output
      if(enabled){
        outputColor = vec4(mix(finalColor, vec3(.0), opacity) + inputColor.rgb, inputColor.a);
      } else {
        outputColor = vec4(inputColor);
      }
    }
  `
  )
};
var LensFlareEffect = class extends Effect {
  constructor({ blendFunction, enabled, glareSize, lensPosition, screenRes, starPoints, flareSize, flareSpeed, flareShape, animated, anamorphic, colorGain, lensDirtTexture, haloScale, secondaryGhosts, aditionalStreaks, ghostScale, opacity, starBurst }) {
    super("LensFlareEffect", LensFlareShader.fragmentShader, {
      blendFunction,
      uniforms: /* @__PURE__ */ new Map([
        ["enabled", new Uniform(enabled)],
        ["glareSize", new Uniform(glareSize)],
        ["lensPosition", new Uniform(lensPosition)],
        ["time", new Uniform(0)],
        ["screenRes", new Uniform(screenRes)],
        ["starPoints", new Uniform(starPoints)],
        ["flareSize", new Uniform(flareSize)],
        ["flareSpeed", new Uniform(flareSpeed)],
        ["flareShape", new Uniform(flareShape)],
        ["animated", new Uniform(animated)],
        ["anamorphic", new Uniform(anamorphic)],
        ["colorGain", new Uniform(colorGain)],
        ["lensDirtTexture", new Uniform(lensDirtTexture)],
        ["haloScale", new Uniform(haloScale)],
        ["secondaryGhosts", new Uniform(secondaryGhosts)],
        ["aditionalStreaks", new Uniform(aditionalStreaks)],
        ["ghostScale", new Uniform(ghostScale)],
        ["starBurst", new Uniform(starBurst)],
        ["opacity", new Uniform(opacity)]
      ])
    });
    __publicField(this, "state");
    this.state = {
      blendFunction,
      enabled,
      glareSize,
      lensPosition,
      screenRes,
      starPoints,
      flareSize,
      flareSpeed,
      flareShape,
      animated,
      anamorphic,
      colorGain,
      lensDirtTexture,
      haloScale,
      secondaryGhosts,
      aditionalStreaks,
      ghostScale,
      opacity,
      starBurst
    };
  }
  updateUniform(key, value) {
    const uniform = this.uniforms.get(key);
    if (uniform) {
      uniform.value = value;
    }
  }
  update(_renderer, _inputBuffer, deltaTime) {
    const time = this.uniforms.get("time");
    if (time) {
      time.value += deltaTime;
    }
  }
  updateUniforms() {
    const { enabled, glareSize, lensPosition, screenRes, starPoints, flareSize, flareSpeed, flareShape, animated, anamorphic, colorGain, lensDirtTexture, haloScale, secondaryGhosts, aditionalStreaks, ghostScale, opacity, starBurst } = this.state;
    this.updateUniform("enabled", enabled);
    this.updateUniform("glareSize", glareSize);
    this.updateUniform("lensPosition", lensPosition);
    this.updateUniform("screenRes", screenRes);
    this.updateUniform("starPoints", starPoints);
    this.updateUniform("flareSize", flareSize);
    this.updateUniform("flareSpeed", flareSpeed);
    this.updateUniform("flareShape", flareShape);
    this.updateUniform("animated", animated);
    this.updateUniform("anamorphic", anamorphic);
    this.updateUniform("colorGain", colorGain);
    this.updateUniform("lensDirtTexture", lensDirtTexture);
    this.updateUniform("haloScale", haloScale);
    this.updateUniform("secondaryGhosts", secondaryGhosts);
    this.updateUniform("aditionalStreaks", aditionalStreaks);
    this.updateUniform("ghostScale", ghostScale);
    this.updateUniform("starBurst", starBurst);
    this.updateUniform("opacity", opacity);
  }
  updateState(state) {
    this.state = {
      ...this.state,
      ...state
    };
    this.updateUniforms();
  }
};

// node_modules/threlte-postprocessing/dist/effects/lens-flare/LensFlareEffect.svelte
mark_module_start();
LensFlareEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/lens-flare/LensFlareEffect.svelte";
function LensFlareEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, LensFlareEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new LensFlareEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction, ..._props } = props;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    _effect.updateState(_props);
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  LensFlareEffect_1 = hmr(LensFlareEffect_1, () => LensFlareEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = LensFlareEffect_1[HMR].source;
    set(LensFlareEffect_1[HMR].source, module.default[HMR].original);
  });
}
var LensFlareEffect_default = LensFlareEffect_1;
mark_module_end(LensFlareEffect_1);

// node_modules/threlte-postprocessing/dist/effects/noise/NoiseEffect.svelte
mark_module_start();
NoiseEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/noise/NoiseEffect.svelte";
function NoiseEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, NoiseEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new NoiseEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction, premultiply } = props;
    if (strict_equals(premultiply, void 0, false)) _effect.premultiply = premultiply;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  NoiseEffect_1 = hmr(NoiseEffect_1, () => NoiseEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = NoiseEffect_1[HMR].source;
    set(NoiseEffect_1[HMR].source, module.default[HMR].original);
  });
}
var NoiseEffect_default = NoiseEffect_1;
mark_module_end(NoiseEffect_1);

// node_modules/threlte-postprocessing/dist/effects/outline/OutlineEffect.svelte
mark_module_start();
OutlineEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/outline/OutlineEffect.svelte";
function OutlineEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, OutlineEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  const $selectionContext = () => (validate_store(selectionContext, "selectionContext"), store_get(selectionContext, "$selectionContext", $$stores));
  let selectionLayer = prop($$props, "selectionLayer", 3, 10), props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "camera",
      "scene",
      "selectionLayer"
    ],
    "props"
  );
  const selectionContext = getSelectionContext();
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new OutlineEffect($$props.scene ?? $context().scene ?? void 0, $$props.camera ?? $context().camera ?? void 0, props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    var _a;
    if ($selectionContext().enabled) {
      if ((_a = $selectionContext().selected) == null ? void 0 : _a.length) {
        _effect.selection.set($selectionContext().selected);
        $context().render();
        return () => {
          _effect.selection.clear();
          $context().render();
        };
      }
    }
  });
  user_effect(() => {
    if (strict_equals(selectionLayer(), void 0, false)) {
      _effect.selectionLayer = selectionLayer();
      $context().render();
    }
  });
  user_effect(() => {
    const {
      blendFunction,
      patternTexture,
      width,
      resolutionY,
      resolutionScale,
      resolutionX,
      height,
      kernelSize,
      hiddenEdgeColor,
      edgeStrength,
      multisampling,
      patternScale,
      visibleEdgeColor,
      xRay,
      pulseSpeed,
      blur
    } = props;
    width && height && _effect.setSize(width, height);
    if (strict_equals(resolutionScale, void 0, false)) _effect.setResolutionScale(resolutionScale);
    if (resolutionX && resolutionY) {
      _effect.resolution.width = resolutionX;
      _effect.resolution.height = resolutionY;
    }
    if (kernelSize) _effect.kernelSize = kernelSize;
    if (strict_equals(hiddenEdgeColor, void 0, false)) _effect.hiddenEdgeColor = new Color(hiddenEdgeColor);
    if (strict_equals(visibleEdgeColor, void 0, false)) _effect.visibleEdgeColor = new Color(visibleEdgeColor);
    if (strict_equals(edgeStrength, void 0, false)) _effect.edgeStrength = edgeStrength;
    if (strict_equals(multisampling, void 0, false)) _effect.multisampling = multisampling;
    if (strict_equals(patternScale, void 0, false)) _effect.patternScale = patternScale;
    if (strict_equals(xRay, void 0, false)) _effect.xRay = xRay;
    if (strict_equals(pulseSpeed, void 0, false)) _effect.pulseSpeed = pulseSpeed;
    if (strict_equals(blur, void 0, false)) _effect.blur = blur;
    if (strict_equals(patternTexture, void 0, false)) _effect.patternTexture = patternTexture;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  OutlineEffect_1 = hmr(OutlineEffect_1, () => OutlineEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = OutlineEffect_1[HMR].source;
    set(OutlineEffect_1[HMR].source, module.default[HMR].original);
  });
}
var OutlineEffect_default = OutlineEffect_1;
mark_module_end(OutlineEffect_1);

// node_modules/threlte-postprocessing/dist/effects/ramp/effect.js
var RampShader = {
  fragmentShader: (
    /* glsl */
    `
    uniform int rampType;

    uniform vec2 rampStart;
    uniform vec2 rampEnd;

    uniform vec4 startColor;
    uniform vec4 endColor;

    uniform float rampBias;
    uniform float rampGain;

    uniform bool rampMask;
    uniform bool rampInvert;

    float getBias(float time, float bias) {
      return time / (((1.0 / bias) - 2.0) * (1.0 - time) + 1.0);
    }

    float getGain(float time, float gain) {
      if (time < 0.5)
        return getBias(time * 2.0, gain) / 2.0;
      else
        return getBias(time * 2.0 - 1.0, 1.0 - gain) / 2.0 + 0.5;
    }

    void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
      vec2 centerPixel = uv * resolution;
      vec2 startPixel = rampStart * resolution;
      vec2 endPixel = rampEnd * resolution;

      float rampAlpha;

      if (rampType == 1) {
        vec2 fuv = centerPixel / resolution.y;
        vec2 suv = startPixel / resolution.y;
        vec2 euv = endPixel / resolution.y;

        float radius = length(suv - euv);
        float falloff = length(fuv - suv);
        rampAlpha = smoothstep(0.0, radius, falloff);
      } else {
        float radius = length(startPixel - endPixel);
        vec2 direction = normalize(vec2(endPixel.x - startPixel.x, -(startPixel.y - endPixel.y)));

        float fade = dot(centerPixel - startPixel, direction);
        if (rampType == 2) fade = abs(fade);

        rampAlpha = smoothstep(0.0, 1.0, fade / radius);
      }

      rampAlpha = abs((rampInvert ? 1.0 : 0.0) - getBias(rampAlpha, rampBias) * getGain(rampAlpha, rampGain));

      if (rampMask) {
        vec4 inputBuff = texture2D(inputBuffer, uv);
        outputColor = mix(inputBuff, inputColor, rampAlpha);
      } else {
        outputColor = mix(startColor, endColor, rampAlpha);
      }
    }
  `
  )
};
var RampType;
(function(RampType2) {
  RampType2[RampType2["Linear"] = 0] = "Linear";
  RampType2[RampType2["Radial"] = 1] = "Radial";
  RampType2[RampType2["MirroredLinear"] = 2] = "MirroredLinear";
})(RampType || (RampType = {}));
var RampEffect = class extends Effect {
  constructor({
    /**
     * Type of ramp gradient.
     */
    rampType = RampType.Linear,
    /**
     * Starting point of the ramp gradient in normalized coordinates.
     *
     * Ranges from `[0 - 1]` as `[x, y]`. Default is `[0.5, 0.5]`.
     */
    rampStart = [0.5, 0.5],
    /**
     * Ending point of the ramp gradient in normalized coordinates.
     *
     * Ranges from `[0 - 1]` as `[x, y]`. Default is `[1, 1]`
     */
    rampEnd = [1, 1],
    /**
     * Color at the starting point of the gradient.
     *
     * Default is black: `[0, 0, 0, 1]`
     */
    startColor = [0, 0, 0, 1],
    /**
     * Color at the ending point of the gradient.
     *
     * Default is white: `[1, 1, 1, 1]`
     */
    endColor = [1, 1, 1, 1],
    /**
     * Bias for the interpolation curve when both bias and gain are 0.5.
     *
     * Ranges from `[0 - 1]`. Default is `0.5`.
     */
    rampBias = 0.5,
    /**
     * Gain for the interpolation curve when both bias and gain are 0.5.
     *
     * Ranges from `[0 - 1]`. Default is `0.5`.
     */
    rampGain = 0.5,
    /**
     * When enabled, the ramp gradient is used as an effect mask, and colors are ignored.
     *
     * Default is `false`.
     */
    rampMask = false,
    /**
     * Controls whether the ramp gradient is inverted.
     *
     * When disabled, rampStart is transparent and rampEnd is opaque.
     *
     * Default is `false`.
     */
    rampInvert = false
  } = {}) {
    super("RampEffect", RampShader.fragmentShader, {
      uniforms: /* @__PURE__ */ new Map([
        ["rampType", new Uniform(rampType)],
        ["rampStart", new Uniform(rampStart)],
        ["rampEnd", new Uniform(rampEnd)],
        ["startColor", new Uniform(startColor)],
        ["endColor", new Uniform(endColor)],
        ["rampBias", new Uniform(rampBias)],
        ["rampGain", new Uniform(rampGain)],
        ["rampMask", new Uniform(rampMask)],
        ["rampInvert", new Uniform(rampInvert)]
      ])
    });
    __publicField(this, "state");
    this.state = {
      rampBias,
      rampEnd,
      rampType,
      endColor,
      rampGain,
      rampInvert,
      rampMask,
      rampStart,
      startColor
    };
  }
  updateUniform(key, value) {
    const uniform = this.uniforms.get(key);
    if (uniform) {
      uniform.value = value;
    }
  }
  updateUniforms() {
    const { rampBias, rampEnd, rampType, endColor, rampGain, rampInvert, rampMask, rampStart, startColor } = this.state;
    this.updateUniform("rampEnd", rampEnd);
    this.updateUniform("rampBias", rampBias);
    this.updateUniform("rampType", rampType);
    this.updateUniform("endColor", endColor);
    this.updateUniform("rampGain", rampGain);
    this.updateUniform("rampInvert", rampInvert);
    this.updateUniform("rampMask", rampMask);
    this.updateUniform("rampStart", rampStart);
    this.updateUniform("startColor", startColor);
  }
  updateState(state) {
    this.state = {
      ...this.state,
      ...state
    };
  }
};

// node_modules/threlte-postprocessing/dist/effects/ramp/RampEffect.svelte
mark_module_start();
RampEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/ramp/RampEffect.svelte";
function RampEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RampEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new RampEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    _effect.updateState(props);
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  RampEffect_1 = hmr(RampEffect_1, () => RampEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = RampEffect_1[HMR].source;
    set(RampEffect_1[HMR].source, module.default[HMR].original);
  });
}
var RampEffect_default = RampEffect_1;
mark_module_end(RampEffect_1);

// node_modules/threlte-postprocessing/dist/effects/SMAA/SMAAEffect.svelte
mark_module_start();
SMAAEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/SMAA/SMAAEffect.svelte";
function SMAAEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, SMAAEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new SMAAEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { preset, predicationMode, edgeDetectionMode } = props;
    preset && _effect.applyPreset(preset);
    if (edgeDetectionMode) _effect.edgeDetectionMaterial.edgeDetectionMode = edgeDetectionMode;
    if (predicationMode) _effect.edgeDetectionMaterial.predicationMode = predicationMode;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  SMAAEffect_1 = hmr(SMAAEffect_1, () => SMAAEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = SMAAEffect_1[HMR].source;
    set(SMAAEffect_1[HMR].source, module.default[HMR].original);
  });
}
var SMAAEffect_default = SMAAEffect_1;
mark_module_end(SMAAEffect_1);

// node_modules/threlte-postprocessing/dist/effects/SSAO/SSAOEffect.svelte
mark_module_start();
SSAOEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/SSAO/SSAOEffect.svelte";
function SSAOEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, SSAOEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new SSAOEffect($context().camera ?? void 0, $context().normalPass && !$context().downSamplingPass ? $context().normalPass.texture : null, {
    blendFunction: BlendFunction.MULTIPLY,
    samples: 30,
    rings: 4,
    distanceThreshold: 1,
    distanceFalloff: 0,
    rangeThreshold: 0.5,
    rangeFalloff: 0.1,
    luminanceInfluence: 0.9,
    radius: 20,
    bias: 0.5,
    intensity: 1,
    color: void 0,
    // @ts-ignore
    normalDepthBuffer: downSamplingPass ? downSamplingPass.texture : null,
    resolutionScale: $context().resolutionScale ?? 1,
    depthAwareUpsampling: true,
    ...props
  });
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      resolutionScale,
      blendFunction,
      resolutionX,
      width,
      samples,
      resolutionY,
      height,
      depthAwareUpsampling,
      distanceFalloff,
      distanceScaling,
      distanceThreshold,
      fade,
      color,
      luminanceInfluence,
      minRadiusScale,
      intensity,
      normalDepthBuffer,
      radius,
      // rangeThreshold,
      // rangeFalloff,
      rings,
      worldDistanceFalloff,
      worldDistanceThreshold,
      worldProximityThreshold,
      worldProximityFalloff,
      bias
    } = props;
    width && height && _effect.setSize(width, height);
    if (strict_equals(resolutionScale, void 0, false)) _effect.resolution.scale = resolutionScale;
    if (resolutionX && resolutionY) {
      _effect.resolution.width = resolutionX;
      _effect.resolution.height = resolutionY;
    }
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    if (strict_equals(samples, void 0, false)) _effect.samples = samples;
    if (strict_equals(depthAwareUpsampling, void 0, false)) _effect.depthAwareUpsampling = depthAwareUpsampling;
    if (strict_equals(distanceFalloff, void 0, false)) _effect.ssaoMaterial.distanceFalloff = distanceFalloff;
    if (strict_equals(distanceScaling, void 0, false)) _effect.ssaoMaterial.distanceScaling = distanceScaling;
    if (strict_equals(distanceThreshold, void 0, false)) _effect.ssaoMaterial.distanceThreshold = distanceThreshold;
    if (strict_equals(fade, void 0, false)) _effect.ssaoMaterial.fade = fade;
    if (strict_equals(luminanceInfluence, void 0, false)) _effect.luminanceInfluence = !!luminanceInfluence;
    if (strict_equals(minRadiusScale, void 0, false)) _effect.ssaoMaterial.minRadiusScale = minRadiusScale;
    if (strict_equals(intensity, void 0, false)) _effect.ssaoMaterial.intensity = intensity;
    if (strict_equals(normalDepthBuffer, void 0, false)) _effect.ssaoMaterial.normalDepthBuffer = normalDepthBuffer;
    if (strict_equals(radius, void 0, false)) _effect.radius = radius;
    if (strict_equals(rings, void 0, false)) _effect.rings = rings;
    if (strict_equals(color, void 0, false)) _effect.color = color;
    if (strict_equals(worldDistanceFalloff, void 0, false)) _effect.ssaoMaterial.worldDistanceFalloff = worldDistanceFalloff;
    if (strict_equals(worldDistanceThreshold, void 0, false)) _effect.ssaoMaterial.worldDistanceThreshold = worldDistanceThreshold;
    if (strict_equals(worldProximityThreshold, void 0, false)) _effect.ssaoMaterial.worldProximityThreshold = worldProximityThreshold;
    if (strict_equals(worldProximityFalloff, void 0, false)) _effect.ssaoMaterial.worldProximityFalloff = worldProximityFalloff;
    if (strict_equals(bias, void 0, false)) _effect.ssaoMaterial.bias = bias;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  SSAOEffect_1 = hmr(SSAOEffect_1, () => SSAOEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = SSAOEffect_1[HMR].source;
    set(SSAOEffect_1[HMR].source, module.default[HMR].original);
  });
}
var SSAOEffect_default = SSAOEffect_1;
mark_module_end(SSAOEffect_1);

// node_modules/threlte-postprocessing/dist/effects/scanline/ScanlineEffect.svelte
mark_module_start();
ScanlineEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/scanline/ScanlineEffect.svelte";
function ScanlineEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ScanlineEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let blendFunction = prop($$props, "blendFunction", 19, () => BlendFunction.OVERLAY), density = prop($$props, "density", 3, 1.25);
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new ScanlineEffect({
    density: density(),
    blendFunction: blendFunction()
  });
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    if (strict_equals(density(), void 0, false)) _effect.density = density();
    if (blendFunction()) _effect.blendMode.blendFunction = blendFunction();
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  ScanlineEffect_1 = hmr(ScanlineEffect_1, () => ScanlineEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = ScanlineEffect_1[HMR].source;
    set(ScanlineEffect_1[HMR].source, module.default[HMR].original);
  });
}
var ScanlineEffect_default = ScanlineEffect_1;
mark_module_end(ScanlineEffect_1);

// node_modules/threlte-postprocessing/dist/effects/selective-bloom/SelectiveBloomEffect.svelte
mark_module_start();
SelectiveBloomEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/selective-bloom/SelectiveBloomEffect.svelte";
function SelectiveBloomEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, SelectiveBloomEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  const $selectionContext = () => (validate_store(selectionContext, "selectionContext"), store_get(selectionContext, "$selectionContext", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "lights",
      "selection",
      "selectionLayer",
      "inverted",
      "ignoreBackground"
    ],
    "props"
  );
  const selectionContext = getSelectionContext();
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new SelectiveBloomEffect($context().scene ?? void 0, $context().camera ?? void 0, {
    blendFunction: $$props.blendFunction ?? BlendFunction.ADD,
    ...props
  });
  onMount(() => {
    tick().then(() => {
      _effect.inverted = !!$$props.inverted;
      _effect.ignoreBackground = !!$$props.ignoreBackground;
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    var _a;
    if (strict_equals((_a = $$props.lights) == null ? void 0 : _a.length, 0)) {
      console.warn("SelectiveBloom requires lights to work.");
    }
    if ($$props.lights && $$props.lights.length > 0) {
      $$props.lights.forEach((light) => addLight(light, _effect));
      $context().render();
      return () => {
        $$props.lights.forEach((light) => removeLight(light, _effect));
        $context().render();
      };
    }
  });
  user_effect(() => {
    if (strict_equals($$props.selectionLayer, void 0, false)) {
      _effect.selection.layer = $$props.selectionLayer;
      $context().render();
    }
  });
  user_effect(() => {
  });
  user_effect(() => {
    if (!$selectionContext() && $$props.selection) {
      _effect.selection.set(Array.isArray($$props.selection) ? $$props.selection : [$$props.selection]);
      $context().render();
      return () => {
        _effect.selection.clear();
        $context().render();
      };
    }
  });
  user_effect(() => {
    var _a;
    if ($selectionContext() && $selectionContext().enabled) {
      if ((_a = $selectionContext().selected) == null ? void 0 : _a.length) {
        _effect.selection.set($selectionContext().selected);
        $context().render();
        return () => {
          _effect.selection.clear();
          $context().render();
        };
      }
    }
  });
  user_effect(() => {
    const {
      blendFunction,
      levels,
      kernelSize,
      resolutionY,
      resolutionX,
      luminanceSmoothing,
      luminanceThreshold,
      radius,
      resolutionScale,
      intensity,
      height,
      width,
      mipmapBlur
    } = props;
    _effect.inverted = !!$$props.inverted;
    _effect.ignoreBackground = !!$$props.ignoreBackground;
    width && height && _effect.setSize(width, height);
    intensity && _effect.setIntensity(intensity);
    resolutionScale && _effect.setResolutionScale(resolutionScale);
    if (radius) _effect.mipmapBlurPass.radius = radius;
    if (luminanceSmoothing) _effect.luminanceMaterial.smoothing = luminanceSmoothing;
    if (luminanceThreshold) _effect.luminanceMaterial.threshold = luminanceThreshold;
    if (resolutionX) _effect.resolution.width = resolutionX;
    if (resolutionY) _effect.resolution.height = resolutionY;
    if (levels) _effect.mipmapBlurPass.levels = levels;
    if (kernelSize) _effect.kernelSize = kernelSize;
    _effect.mipmapBlurPass.enabled = !!mipmapBlur;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  function addLight(light, effect) {
    return light.layers.enable(effect.selection.layer);
  }
  function removeLight(light, effect) {
    return light.layers.disable(effect.selection.layer);
  }
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  SelectiveBloomEffect_1 = hmr(SelectiveBloomEffect_1, () => SelectiveBloomEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = SelectiveBloomEffect_1[HMR].source;
    set(SelectiveBloomEffect_1[HMR].source, module.default[HMR].original);
  });
}
var SelectiveBloomEffect_default = SelectiveBloomEffect_1;
mark_module_end(SelectiveBloomEffect_1);

// node_modules/threlte-postprocessing/dist/effects/sepia/SepiaEffect.svelte
mark_module_start();
SepiaEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/sepia/SepiaEffect.svelte";
function SepiaEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, SepiaEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new SepiaEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { blendFunction, intensity } = props;
    if (strict_equals(intensity, void 0, false)) _effect.intensity = intensity;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  SepiaEffect_1 = hmr(SepiaEffect_1, () => SepiaEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = SepiaEffect_1[HMR].source;
    set(SepiaEffect_1[HMR].source, module.default[HMR].original);
  });
}
var SepiaEffect_default = SepiaEffect_1;
mark_module_end(SepiaEffect_1);

// node_modules/threlte-postprocessing/dist/effects/shock-wave/ShockWaveEffect.svelte
mark_module_start();
ShockWaveEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/shock-wave/ShockWaveEffect.svelte";
function ShockWaveEffect_1($$anchor, $$props) {
  var _a;
  check_target(new.target);
  push($$props, true, ShockWaveEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "position"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new ShockWaveEffect(((_a = $context()) == null ? void 0 : _a.camera) ?? void 0, $$props.position, props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const { speed, maxRadius, waveSize, amplitude } = props;
    if (strict_equals(maxRadius, void 0, false)) _effect.maxRadius = maxRadius;
    if (strict_equals(amplitude, void 0, false)) _effect.amplitude = amplitude;
    if (strict_equals(waveSize, void 0, false)) _effect.waveSize = waveSize;
    if (strict_equals(speed, void 0, false)) _effect.speed = speed;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  ShockWaveEffect_1 = hmr(ShockWaveEffect_1, () => ShockWaveEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = ShockWaveEffect_1[HMR].source;
    set(ShockWaveEffect_1[HMR].source, module.default[HMR].original);
  });
}
var ShockWaveEffect_default = ShockWaveEffect_1;
mark_module_end(ShockWaveEffect_1);

// node_modules/threlte-postprocessing/dist/effects/texture/TextureEffect.svelte
mark_module_start();
TextureEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/texture/TextureEffect.svelte";
function TextureEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, TextureEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId",
      "textureSrc",
      "opacity"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new TextureEffect(props);
  onMount(() => {
    tick().then(async () => {
      $$props.textureSrc && await loadTextureFromSrc($$props.textureSrc);
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    if (strict_equals($$props.textureSrc, void 0, false)) loadTextureFromSrc($$props.textureSrc);
    $context().render();
  });
  user_effect(() => {
    if (strict_equals($$props.opacity, void 0, false)) _effect.blendMode.setOpacity($$props.opacity);
    $context().render();
  });
  user_effect(() => {
    const { blendFunction, texture } = (
      // Deprecated
      // aspectCorrection
      props
    );
    if (strict_equals(texture, void 0, false)) _effect.texture = texture;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  async function loadTextureFromSrc(textureSrc) {
    if (!textureSrc) return;
    const texture = await useLoader(TextureLoader).load(textureSrc);
    if ("encoding" in texture) texture.encoding = 3001;
    else
      t.colorSpace = "srgb";
    _effect.texture = texture;
  }
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  TextureEffect_1 = hmr(TextureEffect_1, () => TextureEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = TextureEffect_1[HMR].source;
    set(TextureEffect_1[HMR].source, module.default[HMR].original);
  });
}
var TextureEffect_default = TextureEffect_1;
mark_module_end(TextureEffect_1);

// node_modules/threlte-postprocessing/dist/effects/tilt-shift/TiltShiftEffect.svelte
mark_module_start();
TiltShiftEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/tilt-shift/TiltShiftEffect.svelte";
function TiltShiftEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, TiltShiftEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new TiltShiftEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      blendFunction,
      offset,
      resolutionY,
      resolutionX,
      resolutionScale,
      kernelSize,
      feather,
      bias,
      rotation,
      focusArea
    } = props;
    if (strict_equals(resolutionScale, void 0, false)) _effect.resolution.scale = resolutionScale;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    if (resolutionX) _effect.resolution.width = resolutionX;
    if (resolutionY) _effect.resolution.height = resolutionY;
    if (focusArea) _effect.focusArea = focusArea;
    if (rotation) _effect.rotation = rotation;
    if (bias) _effect.bias = bias;
    if (feather) _effect.feather = feather;
    if (offset) _effect.offset = offset;
    if (kernelSize) _effect.blurPass.kernelSize = kernelSize;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  TiltShiftEffect_1 = hmr(TiltShiftEffect_1, () => TiltShiftEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = TiltShiftEffect_1[HMR].source;
    set(TiltShiftEffect_1[HMR].source, module.default[HMR].original);
  });
}
var TiltShiftEffect_default = TiltShiftEffect_1;
mark_module_end(TiltShiftEffect_1);

// node_modules/threlte-postprocessing/dist/effects/tone-mapping/ToneMappingEffect.svelte
mark_module_start();
ToneMappingEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/tone-mapping/ToneMappingEffect.svelte";
function ToneMappingEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ToneMappingEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new ToneMappingEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      blendFunction,
      adaptive,
      resolution,
      // averageLuminance,
      maxLuminance,
      minLuminance,
      mode,
      // middleGrey,
      // whitePoint,
      adaptationRate
    } = props;
    if (strict_equals(resolution, void 0, false)) _effect.resolution = resolution;
    if (strict_equals(maxLuminance, void 0, false)) _effect.adaptiveLuminanceMaterial.maxLuminance = maxLuminance;
    if (strict_equals(minLuminance, void 0, false)) _effect.adaptiveLuminanceMaterial.minLuminance = minLuminance;
    if (strict_equals(mode, void 0, false)) _effect.mode = mode;
    if (strict_equals(adaptationRate, void 0, false)) _effect.adaptationRate = adaptationRate;
    if (strict_equals(adaptive, void 0, false)) _effect.adaptive = adaptive;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  ToneMappingEffect_1 = hmr(ToneMappingEffect_1, () => ToneMappingEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = ToneMappingEffect_1[HMR].source;
    set(ToneMappingEffect_1[HMR].source, module.default[HMR].original);
  });
}
var ToneMappingEffect_default = ToneMappingEffect_1;
mark_module_end(ToneMappingEffect_1);

// node_modules/threlte-postprocessing/dist/effects/vignatte/VignetteEffect.svelte
mark_module_start();
VignetteEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/vignatte/VignetteEffect.svelte";
function VignetteEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, VignetteEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new VignetteEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    const {
      blendFunction,
      technique,
      darkness,
      eskil,
      offset
    } = props;
    if (strict_equals(technique, void 0, false)) _effect.technique = technique;
    if (strict_equals(darkness, void 0, false)) _effect.darkness = darkness;
    if (strict_equals(eskil, void 0, false)) _effect.eskil = eskil;
    if (strict_equals(offset, void 0, false)) _effect.offset = offset;
    if (blendFunction) _effect.blendMode.blendFunction = blendFunction;
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  VignetteEffect_1 = hmr(VignetteEffect_1, () => VignetteEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = VignetteEffect_1[HMR].source;
    set(VignetteEffect_1[HMR].source, module.default[HMR].original);
  });
}
var VignetteEffect_default = VignetteEffect_1;
mark_module_end(VignetteEffect_1);

// node_modules/threlte-postprocessing/dist/effects/water/effect.js
var WaterShader = {
  fragmentShader: (
    /* glsl */
    `
    uniform float factor;

    void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
      vec2 vUv = uv;
      float frequency = 6.0 * factor;
      float amplitude = 0.015 * factor;
      float x = vUv.y * frequency + time * 0.7; 
      float y = vUv.x * frequency + time * 0.3;
      vUv.x += cos(x + y) * amplitude * cos(y);
      vUv.y += sin(x - y) * amplitude * cos(y);
      vec4 rgba = texture(inputBuffer, vUv);
      outputColor = rgba;
    }
  `
  )
};
var WaterEffect = class extends Effect {
  constructor({ blendFunction = BlendFunction.NORMAL, factor = 0 } = {}) {
    super("WaterEffect", WaterShader.fragmentShader, {
      blendFunction,
      attributes: EffectAttribute.CONVOLUTION,
      uniforms: /* @__PURE__ */ new Map([
        ["factor", new Uniform(factor)]
      ])
    });
    __publicField(this, "state");
    this.state = {
      blendFunction,
      factor
    };
  }
  updateUniform(key, value) {
    const uniform = this.uniforms.get(key);
    if (uniform) {
      uniform.value = value;
    }
  }
  updateUniforms() {
    this.updateUniform("factor", this.state.factor);
  }
  updateState(state) {
    this.state = {
      ...this.state,
      ...state
    };
    if (state.blendFunction)
      this.blendMode.blendFunction = state.blendFunction;
    this.updateUniforms();
  }
};

// node_modules/threlte-postprocessing/dist/effects/water/WaterEffect.svelte
mark_module_start();
WaterEffect_1[FILENAME] = "node_modules/threlte-postprocessing/dist/effects/water/WaterEffect.svelte";
function WaterEffect_1($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, WaterEffect_1);
  const [$$stores, $$cleanup] = setup_stores();
  const $context = () => (validate_store(context, "context"), store_get(context, "$context", $$stores));
  let props = rest_props(
    $$props,
    [
      "$$slots",
      "$$events",
      "$$legacy",
      "composerId"
    ],
    "props"
  );
  const context = getEffectComposerContext($$props.composerId);
  const _effect = new WaterEffect(props);
  onMount(() => {
    tick().then(() => {
      $context().push(_effect);
    });
    return () => {
      _effect.dispose();
    };
  });
  user_effect(() => {
    _effect.updateState(props);
    $context().render();
  });
  var $$pop = pop({ ...legacy_api() });
  $$cleanup();
  return $$pop;
}
if (import.meta.hot) {
  WaterEffect_1 = hmr(WaterEffect_1, () => WaterEffect_1[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = WaterEffect_1[HMR].source;
    set(WaterEffect_1[HMR].source, module.default[HMR].original);
  });
}
var WaterEffect_default = WaterEffect_1;
mark_module_end(WaterEffect_1);
export {
  ASCIIEffect_default as ASCIIEffect,
  BloomEffect_default as BloomEffect,
  BrightnessContrastEffect_default as BrightnessContrastEffect,
  ChromaticAberrationEffect_default as ChromaticAberrationEffect,
  ColorAverageEffect_default as ColorAverageEffect,
  ColorDepthEffect_default as ColorDepthEffect,
  DepthEffect_default as DepthEffect,
  DepthOfFieldEffect_default as DepthOfFieldEffect,
  DotScreenEffect_default as DotScreenEffect,
  FXAAEffect_default as FXAAEffect,
  GodRaysEffect_default as GodRaysEffect,
  GridEffect_default as GridEffect,
  HueSaturationEffect_default as HueSaturationEffect,
  LUTEffect_default as LUTEffect,
  LensFlareEffect_default as LensFlareEffect,
  NoiseEffect_default as NoiseEffect,
  OutlineEffect_default as OutlineEffect,
  PixelationEffect_default as PixelationEffect,
  RampEffect_default as RampEffect,
  SMAAEffect_default as SMAAEffect,
  SSAOEffect_default as SSAOEffect,
  ScanlineEffect_default as ScanlineEffect,
  SelectiveBloomEffect_default as SelectiveBloomEffect,
  SepiaEffect_default as SepiaEffect,
  ShockWaveEffect_default as ShockWaveEffect,
  TextureEffect_default as TextureEffect,
  TiltShiftEffect_default as TiltShiftEffect,
  ToneMappingEffect_default as ToneMappingEffect,
  VignetteEffect_default as VignetteEffect,
  WaterEffect_default as WaterEffect
};
//# sourceMappingURL=threlte-postprocessing_effects.js.map
