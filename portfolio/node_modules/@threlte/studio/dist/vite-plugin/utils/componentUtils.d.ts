export declare const isModule: (attributes: Record<string, string | boolean>) => boolean;
/**
 * Disassembles a Svelte component into its parts: markup, script, script module,
 * and style. The script and style blocks are replaced with dummy values.
 */
export declare const disassembleComponent: (code: string) => Promise<{
    markup: string;
    reassemble: (newCode: string) => Promise<string>;
}>;
