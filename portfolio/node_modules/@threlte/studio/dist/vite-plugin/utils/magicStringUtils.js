import MagicString from 'magic-string';
export const toMagicString = (markup) => {
    return new MagicString(markup);
};
export const recreateMagicString = (markup) => {
    return new MagicString(markup.toString());
};
export const isMagicString = (object) => {
    return object instanceof MagicString;
};
