<script lang="ts">import IconButton from './IconButton.svelte';
import Tooltip from './Tooltip.svelte';
export let icon;
export let label;
export let active = false;
export let warn = false;
export let success = false;
export let error = false;
export let disabled = false;
export let tooltip = '';
</script>

{#if tooltip.length}
  <Tooltip>
    <IconButton
      on:click
      {disabled}
      {active}
      {warn}
      {success}
      {icon}
      {label}
      {error}
    />
    <span slot="tooltip">{tooltip}</span>
  </Tooltip>
{:else}
  <IconButton
    on:click
    {disabled}
    {active}
    {warn}
    {success}
    {icon}
    {label}
    {error}
  />
{/if}
