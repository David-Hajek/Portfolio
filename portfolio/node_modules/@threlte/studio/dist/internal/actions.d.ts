import type { ExtensionActions } from './types';
export declare const createActions: () => {
    addExtensionActions: (scope: string, extensionActions: ExtensionActions<any>) => void;
    removeExtensionActions: (scope: string) => void;
    runAction: (scope: string, actionId: string, state: any, ...args: any[]) => void | Promise<void>;
};
