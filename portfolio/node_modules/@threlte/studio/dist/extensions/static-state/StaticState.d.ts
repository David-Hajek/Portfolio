import { staticStateMetaKey } from '../../config';
import type { StaticStateMeta } from '../../types';
export declare const register: unique symbol;
export declare const unregister: unique symbol;
export declare const accessors: unique symbol;
export declare const cache: unique symbol;
export declare const setValue: unique symbol;
export declare const getCachedValue: unique symbol;
export declare const instances: unique symbol;
/**
 * An abstract class that can be used to create a **static state**, e.g. a
 * configuration container with properties that are automatically added to the
 * Studio UI. Changes of the state are automatically synced to the code.
 *
 * State declared in a static state should not be mutated but only consumed.
 *
 * This class can be used in `.svelte`, `.svelte.ts` and `.svelte.js` files.
 *
 * @example
 * ```ts
 * class SceneConfig extends StaticState {
 *   cameraPosition = $state({ x: 4, y: 5, z: 0 })
 *   color = $state('#fe3d00')
 * }
 * ```
 */
export declare abstract class StaticState {
    static [instances]: Map<Function, Set<StaticState>>;
    static [cache]: Map<Function, Record<string | symbol, any>>;
    readonly [staticStateMetaKey]: StaticStateMeta;
    [accessors]: string[];
    constructor();
    [register](): void;
    [unregister](): void;
    static [setValue](constructor: Function, property: string | symbol, value: any): void;
    static [getCachedValue](constructor: Function, property: string): any;
}
