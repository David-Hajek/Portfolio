import { createRPCClient } from 'vite-dev-rpc';
const getClientRpc = () => {
    if (import.meta.hot) {
        return createRPCClient('threlte-studio', import.meta.hot, {});
    }
    return undefined;
};
export const clientRpc = getClientRpc();
